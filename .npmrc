# TeleShop Bot NPM Configuration
# Optimized for cross-platform deployment

# Performance optimizations
prefer-offline=true
progress=false
audit-level=moderate

# Security settings
fund=false
optional=true

# Platform compatibility
engine-strict=true

# Build optimizations
# Disable unnecessary scripts during CI/CD
ignore-scripts=false

# Production settings (uncomment for production builds)
# production=true
# only=production

# Registry settings (use default npm registry)
registry=https://registry.npmjs.org/

# Platform-specific configurations
# Railway: Enable all features
# Vercel: Optimize for serverless
# Docker: Minimize dependencies in production